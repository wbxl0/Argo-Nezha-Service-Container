diff --git a/src/routes/server.tsx b/src/routes/server.tsx
index ccfd600..9d49f82 100644
--- a/src/routes/server.tsx
+++ b/src/routes/server.tsx
@@ -26,11 +26,10 @@ import {
     TableRow,
 } from "@/components/ui/table"
 import { IconButton } from "@/components/xui/icon-button"
-import { useServer } from "@/hooks/useServer"
 import { joinIP } from "@/lib/utils"
 import { ModelServerTaskResponse, ModelServer as Server } from "@/types"
 import { ColumnDef, flexRender, getCoreRowModel, useReactTable } from "@tanstack/react-table"
-import { useEffect, useMemo } from "react"
+import { useEffect, useMemo, useState } from "react"
 import { useTranslation } from "react-i18next"
 import { toast } from "sonner"
 import useSWR from "swr"
@@ -41,8 +40,6 @@ export default function ServerPage() {
         revalidateOnFocus: false,
         revalidateOnReconnect: false,
     })
-    const { serverGroups } = useServer()
-
     useEffect(() => {
         if (error)
             toast(t("Error"), {
@@ -88,17 +85,6 @@ export default function ServerPage() {
                 return <div className="max-w-24 whitespace-normal break-words">{s.name}</div>
             },
         },
-        {
-            header: t("Group"),
-            accessorKey: "groups",
-            accessorFn: (row) => {
-                return (
-                    serverGroups
-                        ?.filter((sg) => sg.servers?.includes(row.id))
-                        .map((sg) => sg.group.id) || []
-                )
-            },
-        },
         {
             id: "owner",
             header: t("Owner"),
@@ -148,11 +134,6 @@ export default function ServerPage() {
             accessorKey: "host.version",
             accessorFn: (row) => row.host.version || t("Unknown"),
         },
-        {
-            header: t("EnableDDNS"),
-            accessorKey: "enableDDNS",
-            accessorFn: (row) => row.enable_ddns ?? false,
-        },
         {
             header: t("HideForGuest"),
             accessorKey: "hideForGuest",
@@ -181,7 +162,7 @@ export default function ServerPage() {
                 const s = row.original
                 return (
                     <ActionButtonGroup
-                        className="flex gap-2"
+                        className="flex gap-2 shrink-0"
                         delete={{ fn: deleteServer, id: s.id, mutate: mutate }}
                     >
                         <>
@@ -213,9 +194,14 @@ export default function ServerPage() {
         },
     ]
 
+    const [sortByName, setSortByName] = useState(false)
+
     const dataCache = useMemo(() => {
-        return data ?? []
-    }, [data])
+        const list = [...(data ?? [])]
+        return sortByName
+            ? list.sort((a, b) => a.name.localeCompare(b.name, undefined, { sensitivity: "base" }))
+            : list
+    }, [data, sortByName])
 
     const table = useReactTable({
         data: dataCache,
@@ -228,7 +214,17 @@ export default function ServerPage() {
     return (
         <div className="px-3 max-w-7xl mx-auto">
             <div className="flex flex-col sm:flex-row sm:items-center justify-between w-full gap-3 mt-6 mb-4">
-                <h1 className="text-3xl font-bold tracking-tight">{t("Server")}</h1>
+                <div className="flex items-center gap-4">
+                    <h1 className="text-3xl font-bold tracking-tight">{t("Server")}</h1>
+                    <label className="flex items-center gap-1.5 text-sm text-muted-foreground cursor-pointer select-none">
+                        <Checkbox
+                            checked={sortByName}
+                            onCheckedChange={(value) => setSortByName(!!value)}
+                            aria-label="Sort by name A-Z"
+                        />
+                        A-Z
+                    </label>
+                </div>
                 <HeaderButtonGroup
                     className="flex gap-2 flex-wrap shrink-0"
                     delete={{
@@ -288,7 +284,14 @@ export default function ServerPage() {
                             <TableRow key={headerGroup.id}>
                                 {headerGroup.headers.map((header) => {
                                     return (
-                                        <TableHead key={header.id} className="text-sm">
+                                        <TableHead
+                                            key={header.id}
+                                            className={
+                                                header.column.id === "actions"
+                                                    ? "sticky right-0 z-20 bg-background whitespace-nowrap"
+                                                    : "text-sm"
+                                            }
+                                        >
                                             {header.isPlaceholder
                                                 ? null
                                                 : flexRender(
@@ -315,7 +318,14 @@ export default function ServerPage() {
                                     data-state={row.getIsSelected() && "selected"}
                                 >
                                     {row.getVisibleCells().map((cell) => (
-                                        <TableCell key={cell.id} className="text-xsm">
+                                        <TableCell
+                                            key={cell.id}
+                                            className={
+                                                cell.column.id === "actions"
+                                                    ? "sticky right-0 z-10 bg-background whitespace-nowrap"
+                                                    : "text-xsm"
+                                            }
+                                        >
                                             {flexRender(
                                                 cell.column.columnDef.cell,
                                                 cell.getContext(),
